-- 1. 看板種別マスタテーブルの作成
CREATE TABLE IF NOT EXISTS signage_types_master (
    signage_types_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, -- 自動連番ID (看板種別ID)
    signage_type_name TEXT NOT NULL                                        -- 看板種別
);

-- 2. データの登録（袖看板・自立式看板）
INSERT INTO signage_types_master (signage_type_name) 
VALUES 
    ('袖看板'),
    ('自立式看板');
