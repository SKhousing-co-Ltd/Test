function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Vite + React + TypeScript</p>
        <h1>Ready for local development and Vercel deployment.</h1>
        <p className="description">
          This project uses a standard Vite setup with React and TypeScript,
          including npm scripts for development and production builds.
        </p>
        <div className="actions" aria-label="Available npm scripts">
          <code>npm run dev</code>
          <code>npm run build</code>
        </div>
      </section>
    </main>
  );
}

export default App;
