-- 1. 駐輪場種別マスタテーブルの作成
CREATE TABLE IF NOT EXISTS bicycle_parking_types_master (
    bicycle_parking_types_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, -- 自動連番ID (駐輪場種別ID)
    bicycle_parking_type_name TEXT NOT NULL                                        -- 駐輪場種別
);

-- 2. データの登録（平面・ラック・2段ラック（上段）・2段ラック（下段））
INSERT INTO bicycle_parking_types_master (bicycle_parking_type_name) 
VALUES 
    ('平面'),
    ('ラック'),
    ('2段ラック（上段）'),
    ('2段ラック（下段）');
