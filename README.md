# Test

This repository is initialized for future development.

## Git setup

- Common generated files are ignored via `.gitignore`.
- Text files are normalized via `.gitattributes` for consistent line endings across environments.
